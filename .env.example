# Used by docker-compose.yml at the repo root (copy to `.env`).
# Use an absolute path on your machine; the sandbox service bind-mounts this directory.
SANDBOX_HOST_TMP_PATH=/absolute/path/to/JavaSmartGrader/backend/sandbox/tmp
